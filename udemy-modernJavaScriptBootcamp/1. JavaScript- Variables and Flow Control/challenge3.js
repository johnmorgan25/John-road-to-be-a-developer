// classic way

// set age (testing value)
let age = 6;

/*
//calculate isChild - if age is less than 7
isChild = age <= 7;

//calculate isSenior - if age is greater than 65
isSenior = age >= 65;

//print isChild value
console.log(isChild);

//print isSenior value
console.log(isSenior);
*/

// Using If statement
if (age <= 7) {
  console.log("This age is for a child");
}

if (age >= 65) {
  console.log("This age is for a senior");
}
