let isFreezing = false;

// === is the equality operator
// !== is the unequality operator
// < less than operator
// > greater than operator
// <= less than or equal to operator
// >= greater than or equal to operator

let temp = 31;
isFreezing = temp > 32;

console.log(isFreezing);
